"""Single source of truth for the self-bot's help text, so main.py (the
`.راهنما`/`.help` command) and control_bot.py (the panel bot's help
button) can never drift out of sync with each other."""

from config import PREFIX

HELP_TEXT = (
    "**📖 راهنمای کامل قابلیت‌های سلف**\n\n"
    "**پایه:**\n"
    f"`{PREFIX}ping` — تست پینگ\n"
    f"`{PREFIX}status` — وضعیت کلی سلف\n"
    f"`{PREFIX}تاریخ` — تاریخ و ساعت دقیق امروز (شمسی و میلادی)\n\n"
    "**ظاهر اکانت:**\n"
    f"`{PREFIX}clock on` / `{PREFIX}clock off` — ساعت زنده روی نام\n"
    f"`{PREFIX}font <name>` — استایل فونت پیام‌ها (`{PREFIX}font list` برای دیدن لیست)\n"
    f"`{PREFIX}font off` — خاموش کردن استایل فونت\n\n"
    "**استیکر و کوت:**\n"
    f"`{PREFIX}quote` — ساخت عکس کوت (روی یه پیام ریپلای کن)\n"
    f"`{PREFIX}اسکرین` — ساخت استیکر از متن/عکس/ویدیوی ریپلای‌شده (ویدیو = استیکر متحرک)\n\n"
    "**مدیریت گروه:**\n"
    f"`{PREFIX}حذف <تعداد>` — حذف آخرین پیام‌های خودت تو این چت\n"
    f"`{PREFIX}حذف همه` — پاک کردن تمام پیام‌های خودت تو این چت (دسته‌دسته، تا آخر)\n"
    f"`{PREFIX}حذف` (روی یه پیام ریپلای‌شده، بدون عدد) — پاک کردن پیام‌های خودت "
    "از همون پیام تا الان\n"
    f"`{PREFIX}تگ اعضا` — تگ کردن همه‌ی اعضای گروه\n"
    f"`{PREFIX}تگ ادمین` — تگ کردن ادمین‌های گروه\n\n"
    "**مخاطبین:**\n"
    f"`{PREFIX}بلاک <اسم/یوزرنیم/آیدی>` — بلاک کردن یه نفر (رو پیامش ریپلای کن، یا تو پی‌وی خودش بدون آرگومان بزن)\n"
    f"`{PREFIX}آنبلاک <اسم/یوزرنیم/آیدی>` — برداشتن بلاک\n"
    f"`{PREFIX}ایدی <اسم/یوزرنیم/آیدی>` — گرفتن آیدی عددی یه نفر (رو پیامش ریپلای کن برای گرفتن آیدی خودش)\n\n"
    "**پنل:**\n"
    f"`{PREFIX}پنل` — باز کردن پنل دکمه‌ای این چت\n"
    f"`{PREFIX}بستن` — بستن پنل\n\n"
    "**هوش مصنوعی و ترجمه:**\n"
    f"`{PREFIX}ai <سؤال>` — پرسیدن از هوش مصنوعی (نیازمند `OPENROUTER_API_KEY` تو `.env` — رایگانه؛ "
    "تا ۴ تا کلید هم می‌تونی بدی، وقتی یکی به سقف روزانه خورد خودکار میره سراغ بعدی)\n"
    f"`{PREFIX}ترجمه <زبان>` — ترجمه‌ی پیام ریپلای‌شده (مثلاً `{PREFIX}ترجمه انگلیسی`)\n"
    f"`{PREFIX}ترجمه <زبان> <متن>` — ترجمه‌ی متن دلخواه، بدون نیاز به ریپلای\n\n"
    f"`{PREFIX}راهنما` / `{PREFIX}help` — همین راهنما"
)
